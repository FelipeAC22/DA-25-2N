let quantiaDePets = Number(prompt("Quantos bichinhos você tem? "))

if (quantiaDePets === 0) {
    console.log("Que pena! Você pode adotar um pet!")
} else if (quantiaDePets >) {
    while (quantiaNomes < quantiaDePets ) {
        let nomePet = prompt("Digite o nome de cada pet")
    }

}